<div class="newPost">
    <a href="/addnewBlog"><button class="btn btn-primary"> + New Post</button></a>
</div>

{{#if blogs.length}}
<div class="container-dashboard">
     {{#each blogs as |blog|}}
        {{> bloginfo_LoggedUser blog}}
    {{/each}}
</div>
{{/if}}




