<div class="comments">
  {{#each this}}
    <div class="card">
        <h3 class="card-header"></h3>
        <section class="comment card-body">
            <div class="text">
                {{text}}
            </div>
            <div style="float:right">
                <i><strong> Posted by {{user.username}} On  {{format_date date}} </strong></i>
            </div>
        </section>
    </div>
  {{/each}}
</div>