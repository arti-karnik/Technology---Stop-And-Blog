<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/stylesheet.css">
    <title>Technology-StopAndBlog</title>
</head>
<body>
    <nav class="navbar navbar-dark navbar-expand-md justify-content-between">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-nav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse dual-nav w-50 order-1 order-md-0">
                <ul class="navbar-nav">
                    <li {{#if isHome}} class="nav-item active" {{else}} class="nav-item" {{/if}} >
                        <a class="nav-link pl-0" href="/">Home <span class="sr-only">Home</span></a>
                    </li>  
                    {{#if loggedIn}}
                    <li {{#if isDashboard}} class="nav-item active" {{else}} class="nav-item" {{/if}} >
                        <a class="nav-link" href="/Dashboard">Dashboard</a>
                    </li>
                     <li class="nav-item">
                        <a class="nav-link" id="logout">Logout</a>
                    </li>
                    {{else}}
                    <li {{#if isLogin}} class="nav-item active" {{else}} class="nav-item" {{/if}} >
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
            <a class="navbar-title mx-auto d-block text-center order-0 order-md-1 w-50" id="title"> Technology-Stop And Blog </a>
            <div class="navbar-collapse collapse dual-nav w-50 order-2">
                <ul class="nav navbar-nav ml-auto">
                    <li class="nav-item"><a class="nav-link" href="" hidden="true"> Profile </a></li>
                </ul>
            </div>
        </div>
    </nav>
    <main>
    {{{ body }}}
    <p> home: {{isHome}} <br> dashboard: {{isDashboard}}<br> login: {{isLogin}}</p>

    </main>
</body>

{{#if loggedIn}}
  <script src="/js/logout.js"></script>
{{/if}}

</html>